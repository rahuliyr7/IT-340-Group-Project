<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # Redirects requests for files/directories that don't exist back to index.html
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /index.html [L]
</IfModule>
